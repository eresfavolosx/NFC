## 2024-10-24 - Semantic Forms in Modals
**Learning:** Using `<form>` wrappers inside modals significantly improves accessibility and UX by enabling native "Enter to submit" behavior and browser validation bubbles.
**Action:** Always wrap input fields in a `<form>` tag when building modal dialogs, even if handling submission via JavaScript.
